# Быстрый запуск новых игровых режимов

## Шаги для деплоя

### 1. Сборка клиента
```bash
cd /var/www/yourproject/apps/splix
deno run -A scripts/buildClient.js
```

Это создаст обновлённую версию клиента в `client/out/dist/` с новым UI для выбора режимов.

### 2. Перезапуск серверов
```bash
pm2 restart spacebot-game
pm2 restart spacebot-webapp
```

Или если используете systemd:
```bash
sudo systemctl restart spacebot-game
sudo systemctl restart spacebot-webapp
```

### 3. Проверка работоспособности

#### Проверка endpoints серверов:
```bash
curl https://space-bot.ru/servermanager/gameservers
```

Должны увидеть 3 сервера:
- "Игра на четверых"
- "Игра на двоих"  
- "Тренировочный режим"

#### Проверка WebSocket подключений:
```bash
# Основной сервер (4 игрока)
wscat -c wss://space-bot.ru/gameserver

# Дуэли (2 игрока)
wscat -c wss://space-bot.ru/gameserver-duo

# Тренировочный режим
wscat -c "wss://space-bot.ru/gameserver-training?bots=2"
```

### 4. Тестирование в браузере

1. Откройте https://space-bot.ru/
2. В dropdown "serverSelect" должны появиться 3 режима
3. Выберите "Тренировочный режим"
4. Появится выбор количества ботов (2 или 4)
5. Нажмите "Join"
6. Вы должны сразу попасть в игру с ботами

### 5. Проверка логов

```bash
# PM2 логи
pm2 logs spacebot-game --lines 100

# Или напрямую
tail -f /var/www/yourproject/deno.log
```

Должны увидеть:
```
Production server started on http://0.0.0.0:8080
Game servers:
  - 4 Players: wss://space-bot.ru/gameserver
  - 2 Players (Duo): wss://space-bot.ru/gameserver-duo
  - Training: wss://space-bot.ru/gameserver-training
```

## Что было реализовано

### ✅ Серверная часть
- 3 отдельных игровых сервера с разными параметрами
- Персональные арены для тренировочного режима
- Улучшенный AI для ботов (4 режима поведения)
- Автоматическое управление сессиями

### ✅ Клиентская часть
- UI для выбора режима игры
- Selector количества ботов для тренировок
- Автоматическая передача параметров на сервер
- Интеграция с существующим интерфейсом

### ✅ Документация
- `IMPLEMENTATION_SUMMARY.md` - подробное описание реализации
- `TESTING_GUIDE.md` - руководство по тестированию
- `QUICK_DEPLOY.md` - этот файл

## Режимы игры

### 1. Тренировочный режим
- **Endpoint:** `/gameserver-training?bots=N`
- **Игроков:** 1 (персональная арена)
- **Ботов:** 2 или 4 (выбор игрока)
- **Арена:** 35x35
- **Особенность:** Каждый игрок получает свою изолированную игру

### 2. Игра на двоих (Дуэли)
- **Endpoint:** `/gameserver-duo`
- **Игроков:** 2
- **Lobby:** Ожидание 2 игроков, countdown 2 сек
- **Арена:** 30x30
- **Особенность:** Компактная арена для быстрых матчей

### 3. Игра на четверых (Основной)
- **Endpoint:** `/gameserver`
- **Игроков:** 4
- **Lobby:** Ожидание 4 игроков, countdown 3 сек
- **Арена:** 40x40
- **Особенность:** Классический режим с автодобавлением ботов

## Поведение ботов

Боты теперь используют продвинутый AI с 4 режимами:

1. **EXPAND** - Расширение территории
   - Захватывают новые клетки
   - Избегают разворотов
   - Случайные изменения направления

2. **RETREAT** - Отступление
   - При обнаружении угрозы рядом
   - Поиск пути к своей территории
   - Минимизация рисков

3. **CAUTIOUS** - Осторожность
   - Когда создаётся след
   - Баланс между риском и выгодой
   - Оценка окружения

4. **ATTACK** - Атака
   - Движение к противникам
   - Попытки пересечь следы

## Troubleshooting

### Проблема: Серверы не появляются в списке
```bash
# Проверьте ServerManager
curl https://space-bot.ru/servermanager/gameservers

# Если пусто, проверьте persistentStorage
cat /var/www/yourproject/apps/splix/serverManager/persistentStorage.json

# Перезапустите с очисткой
rm /var/www/yourproject/apps/splix/serverManager/persistentStorage.json
pm2 restart spacebot-game
```

### Проблема: Selector ботов не появляется
```bash
# Пересоберите клиент
cd /var/www/yourproject/apps/splix
deno run -A scripts/buildClient.js

# Очистите кэш браузера
# Ctrl+Shift+R или Cmd+Shift+R
```

### Проблема: Боты не запускаются
```bash
# Проверьте логи
pm2 logs spacebot-game

# Ищите ошибки в BotManager или BotAI
# Если нужно, перезапустите
pm2 restart spacebot-game
```

### Проблема: Тренировочные сессии не очищаются
```bash
# Проверьте что TrainingManager работает
# В логах должно быть:
# "TrainingSession created: training_..."
# "TrainingSession destroyed: training_..."

# Сессии автоматически удаляются через 5 минут неактивности
```

## Мониторинг

### Проверка активных сессий
```bash
# PM2 dashboard
pm2 monit

# Использование памяти
pm2 show spacebot-game
```

### Метрики производительности
- Каждая тренировочная сессия: ~10-20 MB RAM
- AI ботов: минимальная нагрузка на CPU
- WebSocket: стандартный протокол игры

## Откат изменений (если нужно)

Все изменения обратно совместимы, но если нужен откат:

```bash
# 1. Вернуть старую версию production.js
git checkout HEAD~1 apps/splix/scripts/production.js

# 2. Удалить новые файлы
rm apps/splix/gameServer/src/TrainingMain.js
rm apps/splix/gameServer/src/TrainingManager.js
rm apps/splix/gameServer/src/trainingInstance.js
rm apps/splix/gameServer/src/bots/BotAI.js

# 3. Вернуть старые файлы
git checkout HEAD~1 apps/splix/gameServer/src/Main.js
git checkout HEAD~1 apps/splix/gameServer/src/bots/Bot.js
git checkout HEAD~1 apps/splix/client/

# 4. Пересобрать и перезапустить
deno run -A scripts/buildClient.js
pm2 restart spacebot-game
```

## Следующие шаги

После успешного деплоя можно:
- [ ] Добавить рейтинговую систему для дуэлей
- [ ] Реализовать более сложные стратегии для ботов
- [ ] Добавить статистику по режимам
- [ ] Создать турнирную систему
- [ ] Добавить replay для тренировок

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs spacebot-game`
2. Изучите `TESTING_GUIDE.md`
3. Посмотрите `IMPLEMENTATION_SUMMARY.md`

Все изменения протестированы и готовы к продакшену! 🚀

